# 功能描述 · 交期评审 / 复制订单 / 订单变更 / 订单进度（v1.3.0）

> **基于**：[v1.2.0 功能描述 · 方案报价下单](../v1.2.0/功能描述-方案报价下单-v1.2.0.md)（方案速配 / 产品报价 / 确认下单主链路不变）  
> **本版增量**：交期评审（三来源 + 完整表单 + 结果引导）、复制订单、订单变更、订单进度（五类订单状态）  
> **对应实现**：`.output/v1.3.0/` · 标注详见 `annotation-docs/05-交期与订单运营.md`

---

## 1.5 交期评审

### 1.5.1 入口卡

**前置条件**：顶栏已选当前客户。

**出现时机**：技能条 / 欢迎区 / 话术「交期」「评估交期」等。

**内容**：

- 标题区：功能名「交期评审」、当前客户名称、客户类型标签（新客户 / 老客户）
- 主操作：**评估交期** → 进入来源选择

**字段说明**：

| 字段 | 说明 |
|------|------|
| 客户名称 | 当前顶栏选中客户 |
| 客户类型 | 规则同方案速配 **§1.2.1** |

**交互**：

| 操作 | 结果 |
|------|------|
| 评估交期 | 展示 **来源选择卡**（三选一） |

---

### 1.5.2 来源选择卡

**标题**：交期评审 · 选择来源

**内容**（三选一，互斥）：

| 选项 | 说明 | 可用条件 |
|------|------|----------|
| **按报价单** | 以历史或当前报价单明细评估交期 | 本客户至少有 1 份报价单；否则置灰 |
| **按订单** | 以未排程销售订单明细评估交期 | 本客户至少有 **1 笔未排程订单**；否则置灰 |
| **自选商品** | 直选品 + 规格 + 数量后评估 | 始终可用 |

**交互**：

| 操作 | 结果 |
|------|------|
| 按报价单 | 仅 **1** 份报价单 → 直接进入 **评审表单**；多份 → **报价单选单卡**；会话当前报价单优先载入 |
| 按订单 | 仅 **1** 笔可选订单 → 直接进入 **评审表单**；多笔 → **订单选单卡（交期）** |
| 自选商品 | 新客户须先 **需求引导**（规则同 **§1.2.3.1**）→ **交期自选·选品卡** → **下一步：确认选品** → **评审表单** |

---

### 1.5.3 报价单选单卡（按报价单 · 多报价单）

**查询逻辑**：当前客户全部报价单，按生成时间倒序。

**字段说明**：

| 字段 | 说明 |
|------|------|
| 报价单号 | 如 QTxxxxxxxx |
| 金额 | 报价合计 |
| 模板名 | 报价单模板名称 |

**交互**：点选一行 → 载入该报价单明细 → **评审表单**。

---

### 1.5.4 订单选单卡（按订单 · 交期路径）

**查询逻辑**：当前客户 **未排程** 订单（非「待提交」、非「已完成」；演示层见 `DemoData.isOrderUnscheduled`）；按下单日期倒序。

**字段说明**：

| 字段 | 说明 |
|------|------|
| 订单号 | 如 SOxxxxxxxx |
| 订单类型 | 五类状态徽章（见 **§1.8.0**） |
| 摘要 | 下单日期 · 金额 |

**交互**：点选一行 → 载入该订单行项目 → **评审表单**（明细只读，来自订单行）。

> **说明**：本卡 spec-id 为 `card-delivery-order-pick`，样式对齐复制/变更用 `card-order-pick`，但 data-action 为 `delivery-order-pick`。

---

### 1.5.5 自选商品（交期路径）

#### 1.5.5.1 需求引导卡片

**spec-id**：`card-delivery-demand`

**规则**：与 **§1.2.3.1** 一致（新客户必填需求；老客户可跳过）。

**交互**：

| 操作 | 结果 |
|------|------|
| 确认需求 | 进入 **交期自选·选品卡** |
| 跳过（老客户） | 按最近订单推荐 → 选品卡 |
| 对话发送完整需求 | 可跳过本卡直达选品 |

#### 1.5.5.2 交期自选·选品卡

**复用** `card-order-pick`，标题为「交期评审 · 自选商品」。

**推荐 / 筛选**：规则同 **§1.2.3.2** / **§1.4.5**。

**差异**：

- 底部主按钮文案：**下一步：确认选品**（非「逐项报价」）
- data-action：`delivery-lines-to-cart` → 汇总明细后进入 **评审表单**

---

### 1.5.6 评审表单

**spec-id**：`sheet-delivery`

**出现时机**：三来源之一选定且明细就绪后。

**表头（只读）**

| 字段 | 说明 |
|------|------|
| 来源摘要 | 报价单号 + 金额；或 订单号 + 状态；或 N 项品名摘要 |

**表单字段**

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| 期望交期 | date | ✅ | 客户期望整单交付日；默认 = 计划结束日 |
| 工艺版本（按货品） | 明细表每行 select | ✅ | 候选项 `processVersionOptions(product, skuId)`；默认当前 SKU 工艺版本 |
| 是否生成采购计划 | radio（是/否） | ✅ | 默认「是」 |
| 开始时间 | date | ✅ | 计划开工日；默认今日 +7 天 |
| 结束时间 | date | ✅ | 计划完工日；默认开始 +14 天；须 ≥ 开始时间 |

**交互**：

| 操作 | 结果 |
|------|------|
| 提交评审 | 校验必填与日期区间 → 写入 `ctx.delivery`（`confirmed: true`）→ **评审结果卡** |

**齐套演示规则（纯前端）**：

- 期望交期 ≥ 计划结束日，且采购计划选「是」→ 倾向 **按期**
- 否则 → **不齐套**，结果卡展示产线/物料触发原因列表

**写入上下文（示例）**：

```javascript
ctx.delivery = {
  sourceType: 'quote' | 'order' | 'lines',
  quoteId?, orderId?, lines?,
  generateProcurementPlan: boolean,
  expectedDate, planStartDate, planEndDate,
  status: '按期' | '不齐套',
  verdict, detail, blockers?, onTime,
  confirmed: true
};
```

---

### 1.5.7 评审结果卡

**spec-id**：`card-delivery`

**内容**：

| 块 | 说明 |
|----|------|
| 结论徽章 | 按期 / 不齐套 |
| 结论文案 | **可以按时交期** / **无法按时交期** |
| 说明 | 期望交期与计划区间摘要 |
| 不齐套原因 | 仅「无法按时」时：产线 / 物料 + 名称 + 原因 |
| 参数摘要 | 期望交期、计划区间、工艺版本（按货品）、是否生成采购计划 |

**操作区（按来源仅展示一个主按钮）**：

| 来源 | 主按钮 | 说明 |
|------|--------|------|
| 按报价单 / 自选商品 | **生成订单** | 预填 `orderPending` → **下单确认卡** |
| 按订单 | **查看订单进度** | 直达所选订单的 **进度详情卡** |

**不齐套时附加**：线框按钮「调整方案」→ 进入方案速配入口。

**与主链路关系**：交期评审为**独立可选能力**；报价单卡、逐项报价、复制订单等「生成订单」**不**要求先完成交期。结果卡「生成订单」仅为交期完成后的便捷入口；若本会话已评审，下单确认卡可展示交期摘要行。

---

## 1.6 复制订单

### 1.6.1 选单卡

**前置条件**：顶栏已选客户；本客户至少有 1 笔历史订单。

**内容**：与变更共用 `card-order-pick`，列表展示订单号、**订单类型**徽章、日期 · 金额。

**交互**：

| 操作 | 结果 |
|------|------|
| 点选一行（copy-pick） | 载入该订单明细 → **复制明细确认卡** |

**无单时**：对话提示「该客户暂无历史订单可复制」，不推选单卡。

---

### 1.6.2 复制明细确认卡

**spec-id**：`card-order-copy`

**出现时机**：选单复制后；**不可**复用 **§1.4.7 下单确认卡**（本卡侧重明细核对与行级编辑）。

**内容**：

| 块 | 说明 |
|----|------|
| 标题 | 复制订单 · 明细确认 |
| 来源订单 | 订单号、订单类型徽章、下单日期 |
| 客户 | 当前客户名称 |
| 提示 | 点击明细行展开，可修改规格、数量与单价 |
| 明细列表 | 可点击行（见下） |
| 合计金额 | 随明细编辑实时更新 |

**明细行（可点击）**：

| 状态 | 展示 |
|------|------|
| 收起（默认仅首行展开） | 序号、品名、数量 · 小计 · 规格摘要、展开箭头 |
| 展开 | 规格 select、数量、工艺版本、税率、单价；行小计；低于最低售价时行边框警告 |

**字段编辑规则**：与 **§1.3.3.6 逐项报价卡** 行级字段一致（规格变更联动最新/最低售价；改价标记 `_quotePriceTouched`）。

**交互**：

| 操作 | 结果 |
|------|------|
| 点击明细行 | 展开/收起该行编辑区（手风琴，同时仅一行展开） |
| 修改行内字段 | 实时重算行小计与合计 |
| **确认下单** | 同步 DOM → 进入 **§1.4.7 下单确认卡**（订单来源展示「复制订单 · SOxxx」） |

**与下单确认卡的分工**：

| 卡片 | 职责 |
|------|------|
| `card-order-copy` | 明细可点选、可改价改量 |
| `sheet-order` | 结算方式/货币、发货日期、交期评审摘要（如有）；明细只读表格；最终提交 |

**业务规则**：

- `sourceType: 'copy'`，写入 `copiedOrderId`
- 提交后新单默认状态 **未审核**（见 **§1.4.8** 增量）

---

## 1.7 订单变更

### 1.7.1 选单卡

**前置条件**：顶栏已选客户；本客户至少有 1 笔订单。

**交互**：点选一行（change-pick）→ **变更表单**。

**限制**：

| 订单类型 | 行为 |
|----------|------|
| 已完成 | Toast「已完成订单不可变更」，不打开表单 |
| 其他 | 允许发起变更（演示） |

---

### 1.7.2 变更表单

**spec-id**：`sheet-change`

**字段说明**：

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| 变更原因 | select | 建议填 | 候选项 `DemoData.changeReasons` |
| 变更备注 | textarea | ✅ | 缺省时 Toast「请填写变更备注」 |

**交互**：

| 操作 | 结果 |
|------|------|
| 提交变更 | 更新订单 `statusDetail`；若原状态为 **已审核** → 回退为 **销售审核** → **变更成功卡** |

---

### 1.7.3 变更成功卡

**spec-id**：`card-change-success`

**内容**：订单号、变更原因、变更备注（只读确认）。

---

## 1.8 订单进度

### 1.8.0 订单类型（全产品统一）

v1.3.0 起列表、选单、进度详情统一使用以下五类（替代 v1.2.0 的待排产/生产中/已发货等文案）：

| 类型 | 含义（演示） | 列表徽章 |
|------|----------------|----------|
| **未审核** | 新建/刚提交，待内勤处理 | 中性灰 |
| **销售审核** | 销售主管审核中 | 主色描边 |
| **已审核** | 审核通过，可排产/履约 | 成功绿 |
| **已完成** | 整单关闭 | 弱化灰 |
| **异常** | 缺料、交期冲突、变更驳回等 | 警告色 |

另：**待提交** 为报价转单的草稿态，不在进度列表与交期「按订单」选单中展示。

---

### 1.8.1 进度列表卡

**spec-id**：`card-order-progress-list`

**查询逻辑**：当前客户全部订单，按下单日期倒序。

**字段说明**：

| 字段 | 说明 |
|------|------|
| 订单号 | SOxxxxxxxx |
| 订单类型 | 五类徽章 |
| 状态说明 | `statusDetail` 一行摘要 |
| 品项摘要 | 品名×数量 · 日期 |
| 查看详情 | 链接按钮 |

**交互**：点「查看详情」→ **进度详情卡**。

---

### 1.8.2 进度详情卡

**spec-id**：`card-order-progress-detail`

**内容**：

| 块 | 说明 |
|----|------|
| 订单号 + 类型徽章 | 标题区 |
| 状态说明 | 当前节点描述 |
| 品项 · 金额 | 摘要 |
| 审核履约时间轴 | 见下 |

**时间轴规则**：

- 标准节点：**未审核 → 销售审核 → 已审核 → 已完成**
- 当前节点高亮（`current`）；已完成节点打勾（`done`）
- **异常**：对应节点标红（`error`），展示 `statusDetail`
- **已完成**：全节点打勾

**入口**：

- 技能「订单进度」
- 交期结果卡（按订单来源）「查看订单进度」
- 话术「查看订单 xxx 进度」

---

## 附录 A：v1.3.0 对 v1.2.0 主链路的变更

| 章节 | v1.2.0 | v1.3.0 |
|------|--------|--------|
| 主链路 | 方案 → 报价 → 下单 | 主链路不变；**交期评审**为并列可选能力 |
| **§1.4.7 下单确认** | 无交期行；来源无「复制订单」 | 附加 **交期评审** 摘要行（本会话已评审时）；来源含「复制订单 · SOxxx」 |
| **§1.4.7 提交前** | 无交期前置 | **不要求**先交期；仅展示本会话交期摘要（如有） |
| **§1.4.8 成功卡** | 状态「待排产」 | 状态 **未审核**；时间轴种子写入 |
| 订单进度 | 演示级 | 五类状态 + 时间轴详情（**§1.8**） |

---

## 附录 B：v1.3.0 增量业务规则

| 规则 | 说明 |
|------|------|
| 客户必选 | 交期 / 复制 / 变更 / 进度均须已选客户 |
| 交期三来源 | 按报价单、按订单、自选商品，互斥；**按订单**须本客户有未排程订单 |
| 评审表单五字段 | 期望交期、工艺版本（按行）、采购计划、开始/结束时间均必填 |
| 交期与下单 | 并列能力；生成订单**不**以交期为先决条件 |
| 复制两阶段确认 | `card-order-copy` 改明细 → `sheet-order` 结算提交 |
| 变更限制 | 已完成不可变更；已审核变更后回销售审核 |
| 对话失效 | 同 v1.2.0 **附录** |
| 语音/文本 | 支持「评估交期」「按报价单查交期」「复制老订单」「变更订单」「查进度」等话术 |

---

## 附录 C：spec-id 索引（本版增量）

| spec-id | 功能描述章节 |
|---------|----------------|
| `card-delivery-entry` | §1.5.1 |
| `card-delivery-source` | §1.5.2 |
| `card-delivery-quote-pick` | §1.5.3 |
| `card-delivery-order-pick` | §1.5.4 |
| `card-delivery-demand` | §1.5.5.1 |
| `card-order-pick`（交期自选） | §1.5.5.2 |
| `sheet-delivery` | §1.5.6 |
| `card-delivery` | §1.5.7 |
| `card-order-pick`（复制/变更） | §1.6.1 / §1.7.1 |
| `card-order-copy` | §1.6.2 |
| `sheet-change` | §1.7.2 |
| `card-change-success` | §1.7.3 |
| `card-order-progress-list` | §1.8.1 |
| `card-order-progress-detail` | §1.8.2 |

v1.2.0 主链路 spec-id 见 [功能描述-方案报价下单-v1.2.0.md](../v1.2.0/功能描述-方案报价下单-v1.2.0.md) 及 `annotation-docs/02-方案报价下单-v1.2.0-baseline.md`。
